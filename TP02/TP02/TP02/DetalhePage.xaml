<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TP02.DetalhePage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand">
            <StackLayout Orientation="Horizontal">
                <Label Text="Nome:" FontSize="Medium" HorizontalOptions="FillAndExpand" />
                <Label Text="{Binding Nome}" FontSize="Medium" FontAttributes="Bold" />
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Idade:" FontSize="Medium" HorizontalOptions="FillAndExpand" />
                <Label Text="{Binding Idade}" FontSize="Medium" FontAttributes="Bold" />
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Profissão:" FontSize="Medium" HorizontalOptions="FillAndExpand" />
                <Label Text="{Binding Profissao}" FontSize="Medium" FontAttributes="Bold" />
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="País:" FontSize="Medium" HorizontalOptions="FillAndExpand" />
                <Label Text="{Binding Pais}" FontSize="Medium" FontAttributes="Bold" />
            </StackLayout>
            <Button x:Name="btnVoltar" Text="Home" Clicked="btnVoltar_Clicked" />            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>